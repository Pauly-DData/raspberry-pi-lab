## 00 SSH Access
SSH (secure shell) is a protocal that lets you login in to another machine over the network and runs commands in the machine's terminal - safe and secure.

### What do we need to login?

1. The IP address — its address on your home network
2. A username on the Pi (e.g. paulipi)
3. A password (or better: an SSH key, for later)